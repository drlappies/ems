class Overtime {
    create = (employeeId, date, from, to, status) => {
        return {
            employee_id: employeeId,
            date: date,
            from: from,
            to: to,
            status, status
        }
    }
}

export default Overtime